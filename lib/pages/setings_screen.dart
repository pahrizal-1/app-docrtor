import 'package:flutter/material.dart';

class SetingsScreens extends StatelessWidget {
  const SetingsScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
